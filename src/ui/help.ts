/** Путь заметки-справки в вольте (корень). */
export const REQUESTS_HELP_PATH = 'Заявки на испытания — инструкция.md';

/** Текст справки. Копия файла в вольте; при отсутствии — создаётся кнопкой «?». */
export const REQUESTS_HELP_MD = `# Заявки на испытания — инструкция

## Общие принципы

- **Одна заявка = один метод испытаний.** Для испытаний по нескольким методам выберите несколько методов сразу при создании — плагин оформит несколько под-заявок с одним общим номером.
- **Номер заявки** — сквозной по году, формат \`{NNN}/{ГГГГ}\` (например, \`2/2026\`). Каждый метод получает два номера:
  - номер заказчику: \`{КодПроекта}-{NNN}/{ГГГГ}-{КодЛаборатории}-{КодМетода}\` (например, \`0-2/2026-GG-GG-M1\`);
  - номер лаборатории: \`{NNN}/{ГГГГ}-{КодМетода}\` (например, \`2/2026-GG-M1\`).
- Номера присваивает **сервер при синхронизации** — сразу после создания локальная заявка номера не имеет.

## Как открыть плагин

В ЦУП СБЕ (магазин): «Установленные» → «Заявки на испытания» → «Открыть».

## Проекты

- Слева — дерево проектов: строка «🗂 Без проекта» и проекты/подпроекты. Выбор проекта фильтрует список заявок.
- Создание проекта: кнопка **«＋»** над деревом. У проекта — уникальный код (как ИД), название, описание. Подпроект создаётся с указанием родительского проекта.
- Редактирование проекта (код/название/описание): кнопка **«✎»** у проекта в дереве (доступна владельцу проекта или admin).

## Как создать заявку

1. Нажмите **«➕ Новая заявка»**.
2. **Объект исследования** — один из двух режимов:
   - **ЕКН (серийная продукция)**: введите номер ЕКН — плагин подтянет данные из справочника (название, толщина и т. п.). Обязательно укажите **номер партии** (целое число). Если данных по ЕКН нет в справочнике — впишите название материала и целевые характеристики вручную.
   - **Экспериментальный образец (без ЕКН)**: заполните название материала, тип («Серийный выпуск» или «Экспериментальный продукт»), толщину образца, идентификатор образца и целевой показатель.
3. **Приоритет**: «Средний» (по умолчанию), «Критичный» или «Блокер».
4. **Цель испытания**: текущий контроль, НИОКР, сертификация или декларирование.
5. При необходимости отметьте **«провести испытания во внешней лаборатории»** и выберите лабораторию.
6. Выберите **проект** (не обязательно) и **группу видимости** (не обязательно).
7. **Методы испытаний**: отметьте галочками один или несколько. Чем больше методов — тем больше будет под-заявок (они получат один общий номер, у каждой под-заявки — свой метод и свои номера). Определяемые показатели подтянутся автоматически.
8. При необходимости добавьте **описание**.
9. Нажмите **«💾 Сохранить»**. Заявка появится в списке со статусом «🟢 Новая».

Обратите внимание: если выбранные методы относятся к разным лабораториям, каждая под-заявка получит свой блок номеров.

## Синхронизация

- Кнопка **«🔄»** (справа вверху) — синхронизация вручную. Также она запускается автоматически при открытии плагина.
- Все созданные и изменённые заявки хранятся локально и отправляются на сервер при синхронизации. Офлайн-правки никуда не пропадут.
- Номер, присвоенный сервером, подставится в заявку при синхронизации.

## Карточка заявки

- Клик по строке заявки в таблице — открывается карточка: номер, проект, группа, объект, статус, приоритет, цель, владелец, даты.
- **Смена статуса** — выпадающий список в карточке: «Новая» → «Принята» → «В работе» → «Завершена».
- **Файлы**: в карточке есть «Прикрепить файл» — файл загружается в хранилище и привязывается к заявке.
- **«✏️ Редактировать»** — правка полей заявки.

## Редактирование заявки и добавление методов

- Редактировать поля можно только у заявки в статусе **«Новая»**.
- Метод заявки зафиксирован навсегда (в форме он отмечен и недоступен для смены).
- **Добавить метод к существующей заявке** можно только если она в статусе «Новая»:
  1. «✏️ Редактировать»;
  2. в блоке **«➕ Добавить методы (создаст под-заявки с тем же номером)»** отметьте нужные методы;
  3. «💾 Сохранить» — для каждого отмеченного метода создастся под-заявка с **тем же номером (NNN)**, но с новым методом.
- Для заявок в работе или завершённых добавление методов недоступно.

## Группы (видимость)

- Кнопка **«👥 Группы»** — создание групп и управление участниками.
- Заявки, привязанные к группе, видят все участники группы. Владелец группы или admin управляет составом группы.

## Справочники

- Кнопка **«📚 Справочники»**: лаборатории, методы, объекты.
- Лаборатории и методы наполняются в подсистеме ЛИМС — в заявках они доступны только для выбора.
- Объекты исследований создаются автоматически при создании заявки либо вручную (кнопка «➕ Объект»).

## Права доступа

- Роли: **viewer** (только просмотр), **editor** (создание и изменение заявок, проектов, групп, объектов), **admin** (управление правами и общий доступ).
- Свой уровень роли виден в настройках плагина, раздел «Права доступа» (управление доступно только admin).

## Типовые вопросы

- **«Почему у новой заявки нет номера?»** — номер присваивает сервер при первой синхронизации; до этого заявка локальная.
- **«Как добавить метод к уже созданной заявке?»** — только для статуса «Новая»: «✏️ Редактировать» → блок «Добавить методы».
- **«Почему для одной заявки несколько строк в таблице?»** — это под-заявки разных методов, разделяющих один общий номер.
`;